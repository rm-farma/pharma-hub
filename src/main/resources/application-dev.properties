# ===== Pharma Hub - Dev Profile =====
# Para rodar localmente:
#   1. cp .env.example .env  (e preencha se necessário)
#   2. ./mvnw quarkus:dev
#
# O perfil "dev" já sobe automaticamente com quarkus:dev.
# As propriedades abaixo sobrescrevem o application.properties.

# -----------------------------------------------
# Banco de Dados
# -----------------------------------------------
quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/pharmahub_dev
quarkus.datasource.username=postgres
quarkus.datasource.password=postgres
quarkus.datasource.jdbc.max-size=5
quarkus.datasource.jdbc.min-size=1

# -----------------------------------------------
# Logging (legível no terminal, sem JSON)
# -----------------------------------------------
quarkus.log.level=DEBUG
quarkus.log.console.json=false
quarkus.log.console.format=%d{HH:mm:ss,SSS} %-5p [%c{3.}] %s%e%n
quarkus.log.category."com.rmfarma".level=DEBUG

# -----------------------------------------------
# API Keys (hardcoded apenas em dev local)
# Em prod, vêm do Secret Manager via Cloud Build.
# -----------------------------------------------
queryhub.security.api-keys.dev-client=dev-api-key-123
queryhub.security.api-keys.test-client=test-api-key-456

# -----------------------------------------------
# Scalar UI + OpenAPI (apenas dev)
# Acesse: http://localhost:8080/q/scalar
# Spec:   http://localhost:8080/q/openapi
# -----------------------------------------------
quarkus.scalar.path=/q/scalar
quarkus.smallrye-openapi.info-title=Pharma Hub API
quarkus.smallrye-openapi.info-version=1.0.0
quarkus.smallrye-openapi.info-description=\
  Repositório centralizado de consultas SQL analíticas expostas via REST.\n\
  Todas as requisições (exceto /health) exigem o header **X-API-Key**.\n\n\
  Use a chave `dev-api-key-123` para testar em desenvolvimento.
quarkus.smallrye-openapi.info-contact-name=RM Farma ? Plataforma
quarkus.smallrye-openapi.info-contact-email=plataforma@rmfarma.com.br
quarkus.smallrye-openapi.security-scheme=apiKey
quarkus.smallrye-openapi.security-scheme-name=ApiKeyAuth
quarkus.smallrye-openapi.security-scheme-description=Chave de API enviada no header X-API-Key
quarkus.smallrye-openapi.api-key-parameter-name=X-API-Key
quarkus.smallrye-openapi.api-key-parameter-in=header

