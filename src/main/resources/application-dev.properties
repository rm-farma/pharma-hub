# ===== Pharma Hub - Dev Profile =====
# Para rodar localmente:
#   1. gcloud auth application-default login
#   2. ./mvnw quarkus:dev
#
# O perfil "dev" já sobe automaticamente com quarkus:dev.
# As credenciais do banco são buscadas diretamente do GCP Secret Manager.

# -----------------------------------------------
# GCP (autenticação via gcloud auth login)
#
# Este project-id é o projeto de EXECUÇÃO dos jobs BigQuery (billing) e
# também o usado pelo Secret Manager — NÃO é o projeto onde vivem os
# dados/table functions consultados. As queries em resources/queries/*.sql
# referenciam a tabela/function pelo nome totalmente qualificado
# (`rm-farma-dw-prod.licenciado.*`), então funciona como uma consulta
# cross-project: a service account de rmfarma-dev precisa ter
# bigquery.dataViewer no dataset `licenciado` de rm-farma-dw-prod e
# bigquery.jobUser aqui em rmfarma-dev.
# -----------------------------------------------
quarkus.google.cloud.project-id=rmfarma-dev

# -----------------------------------------------
# Logging (legível no terminal, sem JSON)
# -----------------------------------------------
quarkus.log.level=INFO
quarkus.log.console.json=false
quarkus.log.console.format=%d{HH:mm:ss,SSS} %-5p [%c{3.}] (%t) %s%e%n
quarkus.log.category."com.rmfarma".level=DEBUG

# -----------------------------------------------
# API Keys (hardcoded apenas em dev local)
# Em prod, vêm do Secret Manager via Cloud Build.
# -----------------------------------------------
queryhub.security.api-keys.dev-client=d572765238d508028f78d576f0597ccabe0a78958a4ebc02
queryhub.security.api-keys.test-client=test-api-key-456

# -----------------------------------------------
# Swagger UI + OpenAPI (apenas dev)
# Acesse: http://localhost:8080/q/swagger-ui
# Spec:   http://localhost:8080/q/openapi
# -----------------------------------------------
quarkus.swagger-ui.always-include=true
quarkus.swagger-ui.path=/q/swagger-ui
quarkus.swagger-ui.theme=flattop
quarkus.swagger-ui.title=Pharma Hub API
quarkus.swagger-ui.footer=Grupo Hiper Saúde — Plataforma de Dados
quarkus.swagger-ui.deep-linking=true
quarkus.swagger-ui.display-operation-id=false
quarkus.swagger-ui.display-request-duration=true
quarkus.swagger-ui.filter=true
quarkus.swagger-ui.with-credentials=false
quarkus.smallrye-openapi.info-title=Pharma Hub API
quarkus.smallrye-openapi.info-version=1.0.0
quarkus.smallrye-openapi.info-description=Repositório centralizado de queries SQL analíticas expostas via REST. Todas as requisições (exceto /health) exigem o header X-API-Key. Use a chave d572765238d508028f78d576f0597ccabe0a78958a4ebc02 para testar em desenvolvimento.
quarkus.smallrye-openapi.info-contact-name=Grupo Hiper Saúde — Plataforma
quarkus.smallrye-openapi.info-contact-email=plataforma@rmfarma.com.br
quarkus.smallrye-openapi.security-scheme=apiKey
quarkus.smallrye-openapi.security-scheme-name=ApiKeyAuth
quarkus.smallrye-openapi.security-scheme-description=Chave de API enviada no header X-API-Key. Em desenvolvimento use d572765238d508028f78d576f0597ccabe0a78958a4ebc02
quarkus.smallrye-openapi.api-key-parameter-name=X-API-Key
quarkus.smallrye-openapi.api-key-parameter-in=header

