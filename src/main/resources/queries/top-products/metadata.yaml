key: top-products
version: "2.0.0"
description: "Top produtos por quantidade vendida no período — consome a table function get_top_products_by_period do BigQuery"
endpoint: "/queries/top-products/execute"
tags:
  - vendas
  - produtos
  - ranking
params:
  - name: cnpj
    type: STRING
    required: true
    description: "CNPJ da loja"
  - name: startDate
    type: DATE
    required: true
    description: "Data início do período"
  - name: endDate
    type: DATE
    required: true
    description: "Data fim do período"
pagination:
  defaultPageSize: 10
  maxPageSize: 100
  allowUnpaged: true
  maxUnpagedRows: 1000
timeoutMs: 30000
dtoClassName: "com.rmfarma.pharmahub.api.dto.response.queries.TopProductDTO"
mapperClassName: "com.rmfarma.pharmahub.infrastructure.mapper.queries.TopProductMapper"
