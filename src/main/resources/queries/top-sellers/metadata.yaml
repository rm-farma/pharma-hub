key: top-sellers
version: "2.0.0"
description: "Top vendedores por faturamento no período — consome a table function get_top_sellers_by_period do BigQuery"
endpoint: "/queries/top-sellers/execute"
tags:
  - vendas
  - vendedores
  - ranking
params:
  - name: cnpj
    type: STRING
    required: true
    description: "CNPJ da loja"
  - name: startDate
    type: DATE
    required: true
    description: "Data início do período"
  - name: endDate
    type: DATE
    required: true
    description: "Data fim do período"
pagination:
  defaultPageSize: 10
  maxPageSize: 50
  allowUnpaged: true
  maxUnpagedRows: 500
timeoutMs: 30000
dtoClassName: "com.rmfarma.pharmahub.api.dto.response.queries.TopSellerDTO"
mapperClassName: "com.rmfarma.pharmahub.infrastructure.mapper.queries.TopSellerMapper"
