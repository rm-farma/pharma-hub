# ===============================================
# env.yaml — pharma-hub (DEV/NONPROD)
# Variáveis NÃO-SENSÍVEIS injetadas no Cloud Run Service.
#
# Variáveis sensíveis (senhas, API Keys de clientes) vêm do
# Secret Manager via --set-secrets no Cloud Build.
#
# IMPORTANTE: Este arquivo NÃO deve ser commitado com valores reais.
#             Está no .gitignore.
#             Use env.yaml.example como referência.
# ===============================================

# Logging
LOG_LEVEL: "DEBUG"
LOG_JSON: "true"
GCP_LOGGING_ENABLED: "true"

