# ===============================================
# env.yaml — pharma-hub (DEV LOCAL)
# Variáveis de ambiente para rodar localmente com quarkus:dev.
#
# IMPORTANTE: Este arquivo NÃO deve ser commitado com valores reais.
#             Está no .gitignore.
#             Use env.yaml.example como referência.
# ===============================================

# Banco de Dados (mesmo host do pharma-manager em dev)
DB_URL: "jdbc:postgresql://104.198.194.196:5432/"
DB_NAME: "base_de_conhecimento"
DB_USERNAME: "gpt"
DB_PASSWORD: "R3deMF@rm2"

# Logging
LOG_LEVEL: "DEBUG"
LOG_JSON: "false"
GCP_LOGGING_ENABLED: "false"

